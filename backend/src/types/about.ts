export default interface AboutState {
    id: string;
    phone?: string;
    githubId: string;
    linkedinId?: string;
    resume?: string;
    profilePicture?: string;
    description?: string;
    designation?: string;
}
